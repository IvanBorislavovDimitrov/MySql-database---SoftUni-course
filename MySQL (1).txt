CREATE DATABASE db;
USE db;

CREATE TABLE t(
id INT(11) PRIMARY KEY NOT NULL AUTO_INCREMENT,
name VARCHAR(50), age INT(11));

INSERT INTO t(name, age) SELECT n, a FROM table2 as t2 WHERE;

UPDATE table2 AS t2 SET age = 1 WHERE;

DELETE FROM table2 WHERE id NOT IN ();

SELECT * FROM table2 AS t ORDER BY t.id ASC;


DELIMITER $$
CREATE PROCEDURE udp_procedure(number INT(11))

BEGIN

START TRANSACTION;

IF()

THEN 

SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = '';

ROLLBACK;

ELSEIF()
ELSE() 
COMMIT;
END IF;

END$$

DELIMITER ;


DELIMITER $$
CREATE FUNCTION udf_function(number INT(11))
RETURNS INT(11)
BEGIN
DECLARE n = INT(11);
SET n := number * 2;
RETURN n;
END$$
DELIMITER ;

DELIMITER $$
CREATE TRIGGER t_trigger
BEFORE INSERT

ON t
FOR EACH ROW

BEGIN

IF()

THEN 
SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = '';

ELSE()
END IF;

END$$
DELIMITER ;